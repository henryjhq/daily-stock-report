# 定时任务设置指南

## 快速设置

编辑 `theme.json` 中的 `schedule` 配置块：

```json
"schedule": {
  "enabled": true,
  "market": "us-stock",
  "time": "08:00",
  "days": "1-5"
}
```

| 字段 | 说明 | 可选值 |
|---|---|---|
| `enabled` | 是否启用定时任务 | `true` / `false` |
| `market` | 目标市场 | `us-stock`(美股) / `hk-stock`(港股) / `a-stock`(A股) |
| `time` | 北京时间触发时间 | `"08:00"` ~ `"23:59"` |
| `days` | 触发日期 | `"1-5"`(工作日) / `"1-7"`(每天) / `"1,3,5"`(周一三五) |

## 激活方式

修改 `theme.json` 后，在对话中输入：

```
/daily-stock-report schedule:setup
```

系统会自动读取配置并创建对应的定时任务。

## 当前已激活任务

- **美股日报**：工作日（周一至周五）北京时间早上 8:00
- **状态**：已激活（7 天后需续期）

## 管理命令

- 查看所有定时任务：输入「执行定时任务」或「查看定时任务」
- 取消定时任务：输入「取消定时任务」
- 暂停/恢复：修改 `theme.json` → `enabled: false` 后取消，再重新 setup

---

*提示：Cron 任务每 7 天自动过期，届时会收到通知，只需重新激活即可。*
